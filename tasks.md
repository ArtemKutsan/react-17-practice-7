# 📘 Настройка системы стилей блога на React (Context API)

## **Шаг 1. Создание контекста**

- Создать файл **BlogSettingsContext.js**
- Импортировать зависимости из React
- Создать контекст через `createContext()`
- Определить провайдер контекста
- Хранить в нём состояние:
  - размера шрифта
  - межстрочного интервала
- Использовать `useState` для управления настройками
- Экспортировать хук `useBlogSettings` для удобного доступа к контексту

---

## **Шаг 2. Создание компонента настроек (SettingsPanel)**

- Создать файл **SettingsPanel.js**
- Импортировать `useBlogSettings`
- Реализовать интерфейс с элементами управления:
  - выпадающий список для размера шрифта
  - выпадающий список для межстрочного интервала
- Добавить обработчики изменения значений
- Обновлять состояние в контексте при выборе новых параметров

---

## **Шаг 3. Создание компонента статьи (Article)**

- Создать файл **Article.js**
- Получить настройки стилей через `useBlogSettings`
- Применить параметры стиля к тексту статьи:
  - корректный размер шрифта
  - корректный line-height
- Следить за правильными единицами измерения

---

## **Шаг 4. Сборка и запуск приложения**

- Обновить **App.js**
- Импортировать:
  - `BlogSettingsProvider`
  - `SettingsPanel`
  - `Article`
- Обернуть нужную часть приложения в `BlogSettingsProvider`
- Разместить `SettingsPanel` и `Article` внутри провайдера
- Запустить приложение (`npm start` или `yarn start`)
- Проверить работу в браузере

---

## **Шаг 5. Стилизация приложения**

- Стилизовать компоненты с помощью **CSS‑модулей**
- Придать интерфейсу аккуратный внешний вид
- Обеспечить единый стиль для панели настроек и статьи
